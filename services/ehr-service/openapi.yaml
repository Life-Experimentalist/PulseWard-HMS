openapi: 3.0.0
info:
  title: PulseWard EHR Service API
  description: API for EHR record CRUD, clinical write integrity, and record timeline history.
  version: 1.2.0
servers:
  - url: http://localhost:5104
    description: Development server
paths:
  /ehr/records:
    get:
      summary: List EHR records with optional patient and tenant filters
      parameters:
        - name: patientId
          in: query
          required: false
          schema:
            type: string
        - name: tenantKey
          in: query
          required: false
          schema:
            type: string
        - name: includeDeleted
          in: query
          required: false
          schema:
            type: boolean
            default: false
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
      responses:
        "200":
          description: Filtered EHR record list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrRecordListResponse"
    post:
      summary: Create EHR record with initial timeline event
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EhrRecordCreateRequest"
      responses:
        "201":
          description: EHR record created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrRecord"
        "400":
          description: Invalid create payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrErrorResponse"
        "409":
          description: Duplicate record id conflict
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrErrorResponse"
  /ehr/records/{id}:
    get:
      summary: Retrieve EHR record by record id
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: includeDeleted
          in: query
          required: false
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: EHR record details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrRecord"
        "404":
          description: Record not found or deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrErrorResponse"
    put:
      summary: Update EHR record with version integrity checks
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EhrRecordUpdateRequest"
      responses:
        "200":
          description: EHR record updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrRecord"
        "400":
          description: Invalid patch payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrErrorResponse"
        "404":
          description: Record not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrErrorResponse"
        "409":
          description: Version conflict or deleted record update attempt
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrErrorResponse"
    delete:
      summary: Soft-delete EHR record and append timeline event
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EhrRecordDeleteRequest"
      responses:
        "204":
          description: EHR record deleted
        "400":
          description: Missing actor role
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrErrorResponse"
        "404":
          description: Record not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrErrorResponse"
  /ehr/records/{id}/timeline:
    get:
      summary: Retrieve immutable timeline events for an EHR record
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Timeline events
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrTimelineResponse"
        "404":
          description: Record not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrErrorResponse"
  /ehr/records/{id}/prescriptions:
    get:
      summary: List prescriptions linked to an EHR record
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Linked prescription list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrPrescriptionListResponse"
        "404":
          description: Record not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrErrorResponse"
    post:
      summary: Create prescription linked to EHR record
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EhrPrescriptionCreateRequest"
      responses:
        "201":
          description: Prescription created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrPrescription"
        "400":
          description: Invalid prescription payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrErrorResponse"
        "404":
          description: Record not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrErrorResponse"
  /ehr/records/{id}/prescriptions/{prescriptionId}/handoff:
    post:
      summary: Mark EHR prescription as handed off to pharmacy touchpoint
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: prescriptionId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EhrPrescriptionHandoffRequest"
      responses:
        "200":
          description: Prescription handoff metadata generated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrPrescriptionHandoffResponse"
        "400":
          description: Invalid handoff payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrErrorResponse"
        "404":
          description: Record or prescription not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrErrorResponse"
  /ehr/records/{id}/prescriptions/{prescriptionId}/status:
    patch:
      summary: Update EHR prescription lifecycle status
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: prescriptionId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EhrPrescriptionStatusRequest"
      responses:
        "200":
          description: Prescription status updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrPrescription"
        "400":
          description: Invalid status payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrErrorResponse"
        "404":
          description: Record or prescription not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EhrErrorResponse"
components:
  schemas:
    EhrRecord:
      type: object
      properties:
        id:
          type: string
          example: rec-1001
        tenantKey:
          type: string
          example: citycare-hospital
        patientId:
          type: string
          example: pat-88
        clinicianId:
          type: string
          example: cln-42
        encounterType:
          type: string
          example: opd
        encounterAt:
          type: string
          format: date-time
          example: "2026-04-02T09:00:00Z"
        clinicalSummary:
          type: string
          example: Fever and severe headache
        diagnosis:
          type: string
          example: Viral fever
        notes:
          type: string
          example: Follow-up in 3 days
        medications:
          type: array
          items:
            type: string
          example:
            - Paracetamol 500mg
        allergies:
          type: array
          items:
            type: string
          example:
            - Penicillin
        attachments:
          type: array
          items:
            type: string
          example:
            - lab-report-1001.pdf
        status:
          type: string
          enum: [active, deleted]
          example: active
        version:
          type: integer
          example: 2
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        deletedAt:
          type: string
          format: date-time
          nullable: true
        createdByRole:
          type: string
          example: doctor
        updatedByRole:
          type: string
          nullable: true
          example: doctor
    EhrRecordListResponse:
      type: object
      properties:
        records:
          type: array
          items:
            $ref: "#/components/schemas/EhrRecord"
        total:
          type: integer
          example: 1
        returned:
          type: integer
          example: 1
        limit:
          type: integer
          example: 50
    EhrRecordCreateRequest:
      type: object
      required:
        - actorRole
        - patientId
        - clinicalSummary
        - encounterAt
      properties:
        id:
          type: string
          example: rec-1001
        actorRole:
          type: string
          enum: [admin, doctor, nurse, operations]
          example: doctor
        tenantKey:
          type: string
          example: citycare-hospital
        patientId:
          type: string
          example: pat-88
        clinicianId:
          type: string
          example: cln-42
        encounterType:
          type: string
          example: opd
        encounterAt:
          type: string
          format: date-time
          example: "2026-04-02T09:00:00Z"
        clinicalSummary:
          type: string
          example: Fever and severe headache
        diagnosis:
          type: string
          example: Viral fever
        notes:
          type: string
          example: Follow-up in 3 days
        medications:
          type: array
          items:
            type: string
        allergies:
          type: array
          items:
            type: string
        attachments:
          type: array
          items:
            type: string
    EhrRecordUpdateRequest:
      type: object
      required:
        - actorRole
      properties:
        actorRole:
          type: string
          enum: [admin, doctor, nurse, operations]
          example: doctor
        expectedVersion:
          type: integer
          example: 1
        clinicianId:
          type: string
          example: cln-42
        encounterType:
          type: string
          example: opd
        encounterAt:
          type: string
          format: date-time
          example: "2026-04-02T09:15:00Z"
        clinicalSummary:
          type: string
          example: Fever reduced after medication
        diagnosis:
          type: string
          example: Viral fever improving
        notes:
          type: string
          example: Continue meds for 2 days
        medications:
          type: array
          items:
            type: string
        allergies:
          type: array
          items:
            type: string
        attachments:
          type: array
          items:
            type: string
    EhrRecordDeleteRequest:
      type: object
      required:
        - actorRole
      properties:
        actorRole:
          type: string
          enum: [admin, doctor, operations]
          example: admin
    EhrTimelineEvent:
      type: object
      properties:
        eventId:
          type: string
          example: 1308dad8-6ee5-43bd-bef8-03a3b8dbd46f
        sequence:
          type: integer
          example: 2
        eventType:
          type: string
          example: ehr.record.updated
        actorRole:
          type: string
          example: doctor
        occurredAt:
          type: string
          format: date-time
        details:
          type: object
          additionalProperties: true
    EhrTimelineResponse:
      type: object
      properties:
        recordId:
          type: string
          example: rec-1001
        totalEvents:
          type: integer
          example: 2
        events:
          type: array
          items:
            $ref: "#/components/schemas/EhrTimelineEvent"
    EhrPrescription:
      type: object
      properties:
        id:
          type: string
          example: rx-1001
        recordId:
          type: string
          example: rec-1001
        tenantKey:
          type: string
          example: citycare-hospital
        patientId:
          type: string
          example: pat-88
        clinicianId:
          type: string
          example: cln-42
        medicationName:
          type: string
          example: Amoxicillin
        dosage:
          type: string
          example: 500mg
        frequency:
          type: string
          example: twice daily
        durationDays:
          type: integer
          example: 5
        notes:
          type: string
          example: Take after meals
        status:
          type: string
          enum: [drafted, handed-off, accepted, dispensing, fulfilled, cancelled, rejected]
          example: handed-off
        handoff:
          $ref: "#/components/schemas/EhrPrescriptionHandoffMetadata"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        createdByRole:
          type: string
          example: doctor
        updatedByRole:
          type: string
          example: operations
    EhrPrescriptionHandoffMetadata:
      type: object
      properties:
        sent:
          type: boolean
          example: true
        target:
          type: string
          nullable: true
          example: pharmacy-service
        referenceId:
          type: string
          nullable: true
          example: handoff-1001
        sentAt:
          type: string
          format: date-time
          nullable: true
    EhrPrescriptionListResponse:
      type: object
      properties:
        recordId:
          type: string
          example: rec-1001
        total:
          type: integer
          example: 1
        prescriptions:
          type: array
          items:
            $ref: "#/components/schemas/EhrPrescription"
    EhrPrescriptionCreateRequest:
      type: object
      required:
        - actorRole
        - medicationName
        - dosage
        - frequency
        - durationDays
      properties:
        id:
          type: string
          example: rx-1001
        actorRole:
          type: string
          enum: [admin, doctor, nurse, operations]
          example: doctor
        clinicianId:
          type: string
          example: cln-42
        medicationName:
          type: string
          example: Amoxicillin
        dosage:
          type: string
          example: 500mg
        frequency:
          type: string
          example: twice daily
        durationDays:
          type: integer
          example: 5
        notes:
          type: string
          example: Take after meals
    EhrPrescriptionHandoffRequest:
      type: object
      required:
        - actorRole
      properties:
        actorRole:
          type: string
          enum: [admin, doctor, nurse, operations]
          example: doctor
        target:
          type: string
          example: pharmacy-service
        referenceId:
          type: string
          example: handoff-1001
    EhrPrescriptionHandoffResponse:
      type: object
      properties:
        prescription:
          $ref: "#/components/schemas/EhrPrescription"
        handoffTouchpoint:
          type: object
          properties:
            endpoint:
              type: string
              example: /api/pharmacy/prescriptions/handoff
            referenceId:
              type: string
              example: handoff-1001
            target:
              type: string
              example: pharmacy-service
    EhrPrescriptionStatusRequest:
      type: object
      required:
        - actorRole
        - status
      properties:
        actorRole:
          type: string
          enum: [admin, doctor, nurse, operations]
          example: operations
        status:
          type: string
          enum: [drafted, handed-off, accepted, dispensing, fulfilled, cancelled, rejected]
          example: fulfilled
    EhrErrorResponse:
      type: object
      properties:
        message:
          type: string
          example: Record version conflict
        code:
          type: string
          example: EHR_RECORD_VERSION_CONFLICT
        details:
          type: object
          additionalProperties: true
