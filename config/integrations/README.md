# Integration Configuration

This folder stores tenant-level integration configuration.

- default-integration-config.json: baseline configuration.
- hospital-specific files should override only required fields.

Validation schema:
contracts/rest/integration-provider-config.schema.json
